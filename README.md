# Subtuber

一个基于 Monorepo 架构的全栈应用，包含主播订阅管理和下载追踪两个独立的 Web 应用。app-main是目前主要的前端页面

## 📋 项目概述

Subtuber 使用 Vite + Vue 3 构建，采用 Monorepo 架构管理多个子应用。

### 核心功能

**🎮 App Main - 主播订阅管理**
- 订阅喜欢的 Twitch 主播
- 实时查看主播直播状态
- 自动获取高光时刻片段
- AI 分析高光内容
- VOD 视频管理和播放

**📥 App TrackDownload - 下载追踪**
- 粘贴下载链接进行追踪
- 查看下载历史记录（最近 5 条）
- 使用跟踪代码快速查询

## 🏗️ 项目结构

```
subtuber/
├── packages/                     # Monorepo 子应用目录
│   ├── app-main/                # 主播订阅管理应用
│   │   ├── src/
│   │   │   ├── views/          # 页面组件
│   │   │   │   ├── Home.vue           # 主播订阅主页
│   │   │   │   ├── StreamerDetail.vue # 主播详情页
│   │   │   │   ├── RecordingDetail.vue# 录播详情页
│   │   │   │   └── Settings.vue       # 设置页面
│   │   │   ├── components/     # 通用组件
│   │   │   │   ├── StreamerCard.vue      # 主播卡片
│   │   │   │   ├── AddStreamerCard.vue   # 添加主播卡片
│   │   │   │   ├── AddStreamerModal.vue  # 添加主播弹窗
│   │   │   │   └── VODList.vue           # VOD 列表组件
│   │   │   ├── api/            # API 接口
│   │   │   │   ├── index.js           # 基础 API
│   │   │   │   └── streamers.js       # 主播相关 API
│   │   │   ├── composables/    # Vue 组合式函数
│   │   │   │   └── useAuth.js         # 认证相关
│   │   │   ├── utils/          # 工具函数
│   │   │   │   ├── dateFormat.js      # 日期格式化
│   │   │   │   └── platform.js        # 平台工具
│   │   │   ├── styles/         # 样式文件
│   │   │   │   └── global.css
│   │   │   ├── App.vue         # 根组件
│   │   │   └── main.js         # 入口文件
│   │   ├── index.html
│   │   ├── vite.config.js      # Vite 配置（端口 3000）
│   │   └── package.json
│   │
│   ├── app-trackdownload/      # 下载追踪应用
│   │   ├── src/
│   │   │   ├── views/
│   │   │   │   ├── Home.vue           # 追踪主页
│   │   │   │   └── Settings.vue       # 设置页面
│   │   │   ├── api/
│   │   │   │   └── index.js           # API 接口
│   │   │   ├── styles/
│   │   │   │   └── global.css
│   │   │   ├── App.vue
│   │   │   └── main.js
│   │   ├── index.html
│   │   ├── vite.config.js      # Vite 配置（端口 3001）
│   │   └── package.json
│   │
│   └── shared-auth/            # 共享认证模块
│       ├── index.js            # 认证逻辑
│       ├── Login.vue           # 登录组件
│       └── package.json
│
├── archive/                    # 归档的旧版本代码
├── Dockerfile                  # Docker 镜像构建文件
├── nginx.conf                  # Nginx 配置文件
├── package.json                # 根 package.json（workspace 配置）
└── README.md

```

## 🚀 快速开始

### 环境要求

- Node.js 18+
- npm 或 yarn

### 安装依赖

在项目根目录执行：

```bash
npm install
```

这会自动安装所有 workspace 的依赖。

### 开发模式

**启动主播订阅应用：**
```bash
npm run dev:app-main
```
应用运行在 `http://localhost:3000`

**启动下载追踪应用：**
```bash
npm run dev:app-trackdownload
```
应用运行在 `http://localhost:3001`

**同时启动所有应用：**
```bash
npm run dev:all
```

### 后端服务

前端应用需要配合后端 API 使用，后端服务默认运行在 `http://localhost:8080`。

开发模式下，Vite 会自动将 `/api` 和 `/img` 请求代理到后端服务。

## 🔨 构建部署

### 构建单个应用

**构建主播订阅应用：**
```bash
npm run build:app-main
```
产物输出到 `packages/app-main/dist`

**构建下载追踪应用：**
```bash
npm run build:app-trackdownload
```
产物输出到 `packages/app-trackdownload/dist`

### Docker 部署

项目包含完整的 Docker 配置，支持一键构建和部署：

```bash
# 构建 Docker 镜像
docker build -t subtuber:latest .

# 运行容器
docker run -d -p 80:80 --name subtuber subtuber:latest
```

**访问应用：**
- 主播订阅：`http://localhost/app-main`
- 下载追踪：`http://localhost/app-trackdownload`

### 手动部署

1. **构建所有应用**
```bash
npm run build:app-main
npm run build:app-trackdownload
```

## 📝 开发注意事项

- 使用 `shared-auth` 模块处理跨应用的认证逻辑
- 开发时确保后端服务在 `http://localhost:8080` 运行
- 图片资源通过 `/img/proxy` 代理避免 CORS 问题
- 主播状态支持自动刷新（可配置刷新间隔）

## 🤝 贡献指南

1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 开启 Pull Request

## 📄 License

MIT
